---
version: api/v1
kind: BPApplicationEnv
metadata:
  name: dev
spec:
  environment_master: dev
  application: e2e-poc-application
  cluster: e2e-poc-cluster
  namespace: buildpiper-demo-dev
  manual_build: true
  manual_deploy: true
  registry: nexus
  # vault_access_token: 
  # sonar_access_token: 